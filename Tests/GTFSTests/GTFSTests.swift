import XCTest
@testable import GTFS

final class GTFSTests: XCTestCase {
    func testGTFSInit() throws {
        // Note: Currently SwiftPM doesn't support fixtures, so having consistent tests is difficult.
        // let _ = try! GTFS(path: "/Users/emma/Projects/bus-gtfs-static")
        
    }
}
